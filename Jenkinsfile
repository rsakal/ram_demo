pipeline {
  agent ony
  stages {
    stage("build") {
      steps {
        echo 'Building the application now ...'
      }
    }
    stage("test") {
      steps {
        echo 'Testing the application now ...'
      }
    }
    stage("deploy") {
      steps {
        echo 'Deploying the application now ...'
      }
    }
  }
}
